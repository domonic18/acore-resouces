# Claude Code 工作说明

## 项目概述

这是一个《魔兽世界》资源整理项目，核心资产是三张大容量 Excel 清单（NPC、宠物、坐骑）以及对应的预览图/图标目录。Claude Code 在本项目中的常见任务是：维护文档、更新 `.xlsx` 元数据、管理 Git 版本、校验文件路径一致性。

## 关键文件与目录

- `NPC列表.xlsx`、`宠物列表.xlsx`、`坐骑列表.xlsx`：核心数据源，体积大（共约 730 MB+），修改需谨慎。
- `mounts/`、`npcs/`、`pets/`：资源预览图目录，子目录命名通常为 `<模型名><中文名><类型>`。
- `icons/interface/icons/`：`.blp` 图标文件；`icon_inv.txt` / `icon_spell.txt` 为图标名称索引。
- `README.md`：项目说明文档。
- `CLAUDE.md`：本文件。

## 工作约定

1. **优先编辑文本/文档**：对 `.md`、`.txt`、`.json` 的修改可以立即执行并提交。
2. **谨慎处理 `.xlsx`**：
   - 读取/修改前确认文件路径正确。
   - 修改后必须通过 Git 提交并打标签（如用户要求“版本创建”）。
   - 避免同时修改多个大文件，除非确有必要。
3. **路径一致性**：如果新增或重命名了图片目录，应同步更新对应 `.xlsx` 中引用的路径或文件名。
4. **忽略本地配置**：`.vscode/`、`.DS_Store`、macOS 资源分叉等不应进入版本控制。

## 常用命令

```bash
# 查看仓库状态
git status

# 提交文档或数据更新
git add README.md CLAUDE.md *.xlsx
git commit -m "更新说明：xxx"

# 为 xlsx 数据快照创建版本标签
git tag -a v1.0.0 -m "NPC/宠物/坐骑列表 v1.0.0"

# 查看标签列表
git tag -l
```

## 安全与性能提示

- `.xlsx` 文件体积大，读取时避免一次性加载整个工作簿到内存；如仅需检查结构，优先读取 sheet 名称或前 N 行。
- 不要执行会覆盖或删除原始 `.xlsx` 的操作，除非用户明确授权。
- 图像资源目录总容量可能超过数 GB，批量操作前请先确认范围和目标。

## 与其他系统的交互

- 项目当前为本地 Git 仓库；如需推送到远程，请先配置 remote 并考虑启用 Git LFS 管理大文件。
